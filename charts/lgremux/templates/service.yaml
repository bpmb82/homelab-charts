apiVersion: v1
kind: Service
metadata:
  name: {{ include "lgremux.fullname" . }}
  labels:
    {{- include "lgremux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lgremux.selectorLabels" . | nindent 4 }}
