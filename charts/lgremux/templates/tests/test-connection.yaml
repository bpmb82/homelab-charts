apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lgremux.fullname" . }}-test-connection"
  labels:
    {{- include "lgremux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lgremux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
