apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "forgejo-runner.fullname" . }}-test-connection"
  labels:
    {{- include "forgejo-runner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "forgejo-runner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
